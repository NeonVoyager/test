<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oneblog.mapper.CommentMapper">

    <select id="selectByArticleId" resultType="com.oneblog.entity.Comment">
        SELECT * FROM comment
        WHERE article_id = #{articleId} AND status = 1 AND deleted = 0
        ORDER BY create_time ASC
    </select>

    <select id="selectLatestComments" resultType="com.oneblog.entity.Comment">
        SELECT * FROM comment
        WHERE status = 1 AND deleted = 0
        ORDER BY create_time DESC
        LIMIT #{limit}
    </select>

</mapper>

